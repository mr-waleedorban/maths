"use client";

import React, { useEffect, useState } from "react";
import Link from "next/link";
import { useAuth } from "@/context/AuthContext";
import { GRADE_OPTIONS, getGradeLabel } from "@/lib/grades";
import MathText from "@/components/MathText";
import {
  BookOpen,
  FileText,
  MessageSquare,
  Sparkles,
  ArrowLeft,
  Users,
  Award,
  CheckCircle2,
  Download,
  Bell,
  ChevronLeft,
  GraduationCap
} from "lucide-react";

export default function HomePage() {
  const { user, setIsAuthModalOpen } = useAuth();
  const [announcements, setAnnouncements] = useState<any[]>([]);
  const [featuredPdfs, setFeaturedPdfs] = useState<any[]>([]);
  const [featuredForum, setFeaturedForum] = useState<any[]>([]);
  const [stats, setStats] = useState({
    totalStudents: 120,
    totalPdfs: 18,
    totalQuestions: 85,
    totalForum: 42,
  });

  useEffect(() => {
    // Fetch home data logic goes here
  }, []);

  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">الصفحة الرئيسية</h1>
      <p>عدد الطلاب: {stats.totalStudents}</p>
    </main>
  );
}